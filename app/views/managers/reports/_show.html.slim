= stylesheet_link_tag :administration

#report_preview.html_preview[data-user="#{Reports::UserSerializer.new(@user_membership, assign: @assign).to_json}"
                             data-results="#{(@results.map { |a| AssignSerializer.new(a, membership: @user_membership)}).to_json}"
                             data-data="#{ReportSerializer.new(@resource, membership: @user_membership).to_json(include: '**')}"
                             data-locales=@translations.to_json]

= vite_javascript_tag 'entrypoints/report/preview.jsx'
