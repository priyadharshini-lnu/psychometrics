p
  | Welcome 
  = @email
  | !
p
  | You can confirm your account email through the link below:
row
  columns
    .radius.small
      button href= confirmation_url(@resource, confirmation_token: @token, domain: Settings.domain, subdomain: @resource.try(:clients).try(:first).try(:subdomain))
        = 'Confirm my account'
