p Dear #{@user.decorate.full_name},
p Approver of the following report has requested changes and requires your action.

p Project: #{@user_report.project.name}
p Campaign: #{@user_report.campaign.name}
p Report: #{@user_report.report.name}
p Candidate: #{@user_report.user.decorate.full_name}

p You are receiving this email as you are added as a QC for above report which requires QC.

p You can access the report via the following link.

= link_to 'Report', administration_project_new_campaign_url(@user_report.project, @user_report.campaign) + "/user_reports/#{@user_report.id}"
