javascript:
  var PsyGlobalState = {
    realEnv: "#{ENV['REAL_ENV']}",
    currentUser: #{(current_user || {}).to_json(only: [:id, :email]).html_safe},
    sentryClientDns: "#{ENV['SENTRY_CLIENT_DNS']}"
  }
