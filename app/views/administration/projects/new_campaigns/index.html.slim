div id="#{project.migrated ? 'project-container' : 'campaigns-container'}"

javascript:
  window.__INITIAL_STATE__ = #{@init_state.to_json.html_safe}

- if project.migrated?
  = vite_javascript_tag 'entrypoints/admin/project.jsx'
- else
  = vite_javascript_tag 'entrypoints/admin/campaign.jsx'
