ul.x-navigation.x-navigation-panel id="sidebar_#{dom_id(@resource)}"
  li.xn-title = t('.title')

  - if policy(@resource).edit?
    li = link_to t('.edit'), [:edit, :administration, :dimension, :factor, { id: @resource.id, dimension_id: @resource.dimension_id }], remote: true