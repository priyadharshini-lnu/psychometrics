$(function () {
  $('#modal-container').find('.modal').modal('hide');
  $(document).trigger('close_sidebar');
// Notify message
  noty({
    text: '<%= t(".successfully") %>',
    type: 'success'
  });
});

