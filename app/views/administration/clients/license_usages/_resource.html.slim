tr id=dom_id(resource)
  td = resource.id
  td
    - if resource.campaign&.threesixty?
      =link_to(resource.campaign.name, administration_client_project_threesixty_campaign_path(client, resource.campaign.project, resource.campaign.threesixty_campaign))
    - if resource.campaign&.common?
      =link_to(resource.campaign.name, administration_project_new_campaign_path(resource.campaign.project, resource.campaign))
    - else
      = resource.extras['campaign_name']
  td = resource.extras['subject_name']
  td = resource.extras['subject_email']
  td = resource.decorate.formatted_date_time(:created_at)
  td = resource.decorate.formatted_date_time(:status_updated_at)
  td = resource.decorate.status_updated_by_email
  td.actions-cell
    = link_to action_label, toggle_activation_status_administration_client_license_license_usage_path(id: resource.id),
        remote: true, class: 'btn btn-default btn-rounded btn-sm', method: 'patch'
