$('#modal-container').find('.modal').modal('hide');
$('#modal-container').find('.modal').on('hidden.bs.modal', function(){
  $('#<%= dom_id(@resource) %>').hide("fast", function(){
    var resource = $('<%= j render(partial: @resource.decorate.resource_partial, locals: { resource: @resource }) %>').hide();
    $(this).replaceWithPush(resource).addClass('active').show('slow');
    $(document).trigger('initPlugins');
  });
});
// Notify message
noty({text: '<%= t("administration.#{@resource_class.model_name.plural}.update.successfully", name: @resource.decorate.display_name, type: @resource.get_type) %>', type: 'success'});
