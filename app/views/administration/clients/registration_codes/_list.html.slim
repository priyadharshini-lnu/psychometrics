.table-responsive
  table.table.table-bordered.table-actions.table-hover.selectable.list id="#{resource_class.model_name.plural}_list"
    thead
      tr
        th.text-center width='1%' = link_to_sort resource_class, :active, filter_form
        th = link_to_sort resource_class, :name, filter_form
        th = link_to_sort resource_class, :code, filter_form
        th = link_to_sort resource_class, :start_date, filter_form
        th = link_to_sort resource_class, :end_date, filter_form
        th = link_to_sort resource_class, :usage_stats, filter_form
        th style="width:1%" = link_to_sort resource_class, :actions, filter_form
    tbody
      - if resources.present?
        = render partial: 'resource', collection: resources
      - else
        tr.no_records
          td align='center' colspan='10' = t('administration.no_data_found')
  = paginate resources, theme: 'twitter-bootstrap-3'
