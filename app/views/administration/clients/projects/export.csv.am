csv << [
        resource_class.human_attribute_name('name'),
        t('.project_admin'),
        resource_class.human_attribute_name('number'),
        t('.tests_allocated'),
        t('.users_count'),
        t('.archive_status')
       ]

resources.each do |resource|
  row = [
          resource.decorate.display_name,
          resource.decorate.array_project_admins.join(', '),
          resource.number,
          resource.decorate.array_reports.join(', '),
          resource.end_memberships.size,
          resource.decorate.status
         ]
  csv << row
end
