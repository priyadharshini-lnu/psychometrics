$('#filterrific_results').html('<%= j render(partial: 'list') %>');

// Change form object
$('#filterrific_sorted_by').val('<%= @filterrific.sorted_by %>');
$('#filterrific_with_parent').val('<%= @filterrific.with_parent %>');

// Reinit all plugins
$(document).trigger('initPlugins');
