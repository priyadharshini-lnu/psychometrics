$('#filter_results, #filterrific_results').html('<%= j render(partial: 'list') %>');

// Change form object
// TODO: Remove when will be totaly use Ransack
$('#filterrific_sorted_by').val('<%= @filterrific.try(:sorted_by) %>');

// Reinit all plugins
$(document).trigger('initPlugins');
