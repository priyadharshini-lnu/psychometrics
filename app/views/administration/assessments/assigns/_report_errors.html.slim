- if report_errors.present?
  .alert.alert-danger
    strong Unassigned reports:
    - Report.where(id: report_errors.keys).each do |report|
      br
      p
        strong= report.decorate.display_name
        br
        = report_errors[report.id].join(', ')
