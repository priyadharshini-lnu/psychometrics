ul.x-navigation.x-navigation-panel id="sidebar_#{dom_id(@resource)}"
  li.xn-title.title = t('.title', id: @resource.id)
  - if policy(@resource).edit?
    li = link_to t('.edit'), edit_administration_dimension_factor_sub_factor_path(@dimension, @factor, @resource), remote: true
    - if policy(@resource).new?
    li = link_to t('administration.create'), new_administration_dimension_factor_sub_factor_path(@dimension, @factor), remote: true
