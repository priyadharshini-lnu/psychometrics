
csv << headers

users.each do |user|
  row = [
          user.campaign_users.find { |cu| cu.campaign_id == campaign.id }.decorate.status_text,
          user.first_name,
          user.last_name,
          user.email,
          user.locale,
          nil,
          user.decorate.created_at,
         ]
  csv << row
end
