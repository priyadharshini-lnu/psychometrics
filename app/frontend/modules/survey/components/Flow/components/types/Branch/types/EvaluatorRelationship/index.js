import Editor from './EvaluatorRelationship'
import Preview from './EvaluatorRelationshipPreview'
import connect from './connect'

export default { Editor: connect(Editor), Preview: connect(Preview) }
