import React, { Attributes } from 'react'

interface Props extends Attributes {
  prefix?: string
}

const Sequencing: React.FC<Props> = () => (
  <div>Under development</div>
)

export default Sequencing
