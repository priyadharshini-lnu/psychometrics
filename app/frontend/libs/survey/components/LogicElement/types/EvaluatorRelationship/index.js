import Select from './EvaluatorRelationship'
import Preview from './EvaluatorRelationshipPreview'
import connect from './connect'

export default { Select: connect(Select), Preview: connect(Preview) }
