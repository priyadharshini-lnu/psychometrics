#!/usr/bin/env bash

if ! foreman version &> /dev/null
then
  echo "Installing foreman..."
  gem install foreman
fi
dev="dev"
env="${1:-$dev}"

echo "Run with Procfile.$env"
foreman start -f Procfile.$env "$@"
