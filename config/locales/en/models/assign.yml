en:
  activerecord:
    models:
      assign: 'Assigns'
    attributes:
      assign: &DEFAULT
        completed_at: 'Completion Date'
        statuses:
          not_started: 'New'
          in_progress: 'In Progress'
          'completed': 'Completed'
  simple_form:
    placeholders:
      assessment:
        <<: *DEFAULT
