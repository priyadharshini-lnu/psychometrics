en:
  activerecord:
    models:
      assign: 'Assigns'
    attributes:
      assign: &DEFAULT
        completed_at: 'Completion Date'
        statuses:
          not_started: 'New'
          in_progress: 'In Progress'
          'completed': 'Completed'
    errors:
      messages:
        not_uniqueness: 'Assign has already been taken'          
  simple_form:
    placeholders:
      assessment:
        <<: *DEFAULT
